import { useDispatch } from 'react-redux';

import {
    changeData,
} from './listGamesSlice';

export const LoginForm = () => {

    const dispatch = useDispatch();

    return (
        Profile
    );
}

